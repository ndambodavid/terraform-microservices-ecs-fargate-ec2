# --- Terraform ---
# Ignore local Terraform directories and provider plugins.
# These are generated by `terraform init` and can be recreated.
.terraform/

# Ignore local Terraform state files.
# WARNING: State files contain sensitive information about your infrastructure.
# It's best practice to use a remote backend (like S3) for state management.
*.tfstate
*.tfstate.backup

# Ignore Terraform crash logs.
crash.log
crash.*.log

# Ignore all .tfvars files to prevent committing sensitive variables.
# Instead, create a `terraform.tfvars.example` file to track required variables.
*.tfvars
*.tfvars.json

# Ignore override files, which are meant for local-only adjustments.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore Terraform CLI configuration files.
.terraformrc
terraform.rc

# --- Operating System Files ---
# Ignore macOS specific files.
.DS_Store
._*
.Spotlight-V100
.Trashes

# Ignore Windows specific files.
Thumbs.db
ehthumbs.db
Desktop.ini

# --- IDE / Editor Directories ---
# Ignore configurations for common editors.
.vscode/
.idea/
*.swp
*~